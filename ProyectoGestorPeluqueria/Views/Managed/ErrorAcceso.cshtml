@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container text-center py-5">
    <i class="fas fa-ban fa-4x text-danger mb-4"></i>
    <h1 class="display-4 fw-bold text-danger">Acceso Denegado</h1>
    <p class="lead text-muted mt-3">No tienes permiso para acceder a esta sección.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-4">
        <i class="fas fa-home me-2"></i>Volver al inicio
    </a>
</div>
